public interface CarBuilder {
     void buildCarDoors();
     void buildCarSteering();
     void buildCarMirrors();
     void buildCarWheels();
     Car getCar();
}
//main part of car builder
//method for creating car with parts.
